{{!-- Display the error/message if an error/message is present --}}
{{#if (or error message)}}
<style>
  .banner {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
  }

  .banner>span {
    margin-top: 64px;
    padding: 12px 32px;
    width: min(100%, fit-content);
    height: fit-content;
    border-radius: 5px;
    text-align: center;
  }

  .error {
    background-color: var(--warn-400);
    color: var(--warn-contrast-400);
  }

  .message {
    background-color: var(--info-400);
    color: var(--info-contrast-400);
  }
</style>

<div class="banner">
  {{#if error}}<span class="error">{{ error }}</span>
  {{else}}<span class="message">{{ message }}</span>{{/if}}
</div>

{{else}}

<p>Er du sikker på at du vil framelde dig lan? Husk at du kan altid tilmelde dig igen, men din plads er måske blevet taget.</p>

<form action="/lan/{{ lan._id }}/frameld" method="post">
  <button class="warn" type="submit">Frameld</button>
</form>

{{/if}}