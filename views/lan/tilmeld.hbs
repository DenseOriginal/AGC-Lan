{{!-- Display the error if an error is present --}}
{{#if (or error message)}}
<style>
  .banner {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
  }

  .banner>span {
    margin-top: 64px;
    padding: 12px 32px;
    width: min(100%, fit-content);
    height: fit-content;
    border-radius: 5px;
    text-align: center;
  }

  .error {
    background-color: var(--warn-400);
    color: var(--warn-contrast-400);
  }

  .message {
    background-color: var(--info-400);
    color: var(--info-contrast-400);
  }
</style>

<div class="banner">
  {{#if error}}<span class="error">{{ error }}</span>
  {{else}}<span class="message">{{ message }}</span>{{/if}}
</div>

{{else}}

<p>Vi er super glade for at du gider at komme til LAN. Før vi kan reservere din plads til dette LAN, vil vi gerne vide
  hvor du vil sidde henne. Her er et billede over alle pladserne til lan, klike på lige præcis den plads du vil have for
  at reservere den, hvis du ikke vil have en plads til dine ting, så klik her.</p>

<form action="/lan/{{ lan._id }}/tilmeld" method="post">
  <button type="submit">Tilmeld</button>
</form>

{{/if}}